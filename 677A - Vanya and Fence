#include<iostream>
using namespace std;
int main(){
    int n,h,a;
    cin>>n>>h;
    int count =0;
    for(int i=0; i<n; i++){
        cin>>a;
        if(a>h){
            count+=2;
        }
        else 
            count++;
    }
    cout<<count;
}
