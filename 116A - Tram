#include<iostream>
using namespace std;
int main(){
    int n,a,b;
    cin>>n;
    int count=0,mount=0;
    for(int i=0; i<n; i++)
    {
        cin>>a>>b;
        count += -a+b;
        if(mount <= count)
        {
            mount = count;
        }
    }
    cout<<mount;
}
